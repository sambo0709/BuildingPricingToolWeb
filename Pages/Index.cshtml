@page
@model IndexModel

<main>
    <header>
        <h1>Building Pricing Tool</h1>
        <p>Enter the building details below to calculate an estimate.</p>
    </header>

    <section>
        <h2>Building Details</h2>

        <form method="post">
            <fieldset>
                <legend>Dimensions</legend>

                <p>
                    <label asp-for="Width">Width (ft)</label>
                    <input asp-for="Width" type="number" step="0.01" />
                    <span asp-validation-for="Width" class="validation-message"></span>
                </p>

                <p>
                    <label asp-for="Length">Length (ft)</label>
                    <input asp-for="Length" type="number" step="0.01" />
                    <span asp-validation-for="Length" class="validation-message"></span>
                </p>

                <p>
                    <label asp-for="WallHeight">Wall Height (ft)</label>
                    <input asp-for="WallHeight" type="number" step="0.01" />
                    <span asp-validation-for="WallHeight" class="validation-message"></span>
                </p>

                <p>
                    <label asp-for="RoofPitch">Roof Pitch</label>
                    <input asp-for="RoofPitch" type="number" step="0.01" />
                    <span asp-validation-for="RoofPitch" class="validation-message"></span>
                </p>
            </fieldset>

            <fieldset>
                <legend>Material</legend>

                <p>
                    <label asp-for="MaterialType">Main Material</label>
                    <select asp-for="MaterialType">
                        <option value="Wood">Wood</option>
                        <option value="Steel">Steel</option>
                        <option value="Concrete">Concrete</option>
                    </select>
                </p>
                </fieldset>

                <fieldset>
                <legend>Features</legend>

                <p>
                    <label asp-for="DoorCount">Doors</label>
                    <input asp-for="DoorCount" type="number" />
                    <span asp-validation-for="DoorCount" class="validation-message"></span>
                </p>

                <p>
                    <label asp-for="WindowCount">Windows</label>
                    <input asp-for="WindowCount" type="number" />
                    <span asp-validation-for="WindowCount" class="validation-message"></span>
                </p>
            </fieldset>

            <button type="submit">Calculate Estimate</button>
        </form>
    </section>

    @if (Model.EstimateResult != null)
{
    <section class="results">
        <h2>Estimate Breakdown</h2>

        <h3>Selected Options</h3>
        <p><strong>Material:</strong> @Model.EstimateResult.SelectedMaterial</p>
        <p><strong>Material Rate:</strong> @Model.EstimateResult.MaterialRate.ToString("C") per sq ft</p>

        <h3>Area Calculations</h3>
        <p><strong>Floor Area:</strong> @Model.EstimateResult.FloorArea.ToString("N2") sq ft</p>
        <p><strong>Wall Area:</strong> @Model.EstimateResult.WallArea.ToString("N2") sq ft</p>
        <p><strong>Roof Area:</strong> @Model.EstimateResult.RoofArea.ToString("N2") sq ft</p>
        <p><strong>Total Surface Area:</strong> @Model.EstimateResult.TotalSurfaceArea.ToString("N2") sq ft</p>

        <h3>Cost Breakdown</h3>
        <p><strong>Material Cost:</strong> @Model.EstimateResult.MaterialCost.ToString("C")</p>
        <p><strong>Labor Cost:</strong> @Model.EstimateResult.LaborCost.ToString("C")</p>
        <p><strong>Door Cost:</strong> @Model.EstimateResult.DoorCost.ToString("C")</p>
        <p><strong>Window Cost:</strong> @Model.EstimateResult.WindowCost.ToString("C")</p>
        <p><strong>Total Feature Cost:</strong> @Model.EstimateResult.FeatureCost.ToString("C")</p>

        <h3>Total Estimate</h3>
        <p class="total-cost">@Model.EstimateResult.TotalCost.ToString("C")</p>
    </section>
}
</main>